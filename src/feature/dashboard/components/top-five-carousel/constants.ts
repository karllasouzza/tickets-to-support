import { Dimensions } from "react-native";

export const { width: SCREEN_WIDTH } = Dimensions.get("window");
export const CARD_GAP = 12;
export const CARD_WIDTH = SCREEN_WIDTH - 48;
export const AUTO_PLAY_MS = 3000;
